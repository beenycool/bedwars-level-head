# Copy this file to .env and fill in the required values before starting the backend.
# Lines beginning with # are comments and are ignored.

# Required settings ---------------------------------------------------------
# Hypixel API key owned by you. See https://developer.hypixel.net/api/key for details.
HYPIXEL_API_KEY=YOUR_HYPIXEL_API_KEY_PLACEHOLDER

# Connection string for the PostgreSQL instance that stores cached responses.
# The docker-compose.yml file defaults to a postgres container named "postgres".
CACHE_DB_URL=postgresql://user:password@localhost:5432/levelhead_cache

# Comma-separated list of tokens that can access administrative endpoints.
ADMIN_API_KEYS=YOUR_ADMIN_API_KEY_PLACEHOLDER

# Comma-separated list of tokens that can access cron endpoints.
CRON_API_KEYS=YOUR_CRON_API_KEY_PLACEHOLDER

# Optional tuning -----------------------------------------------------------
#PORT=3000
#HOST=0.0.0.0
#TRUST_PROXY=false
#RATE_LIMIT_MAX=300
#RATE_LIMIT_WINDOW_MS=300000
#PUBLIC_RATE_LIMIT_MAX=60
#PUBLIC_RATE_LIMIT_WINDOW_MS=60000
#CRON_RATE_LIMIT_MAX=10
#CRON_RATE_LIMIT_WINDOW_MS=3600000
# CACHE_TTL_MS is clamped between 1 hour (3600000ms) and 24 hours (86400000ms).
#CACHE_TTL_MS=3600000
#CACHE_DB_POOL_MIN=0
#CACHE_DB_POOL_MAX=10
#HYPIXEL_API_BASE_URL=https://api.hypixel.net
#HYPIXEL_TIMEOUT_MS=5000
#HYPIXEL_RETRY_DELAY_MIN_MS=50
#HYPIXEL_RETRY_DELAY_MAX_MS=150
#CLOUDFLARE_TUNNEL=
#POSTGRES_DB=levelhead_cache
#POSTGRES_USER=levelhead
#POSTGRES_PASSWORD=levelhead
