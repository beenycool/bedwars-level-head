# Copy this file to `.env` and fill in real values for local development.
# Production deployments should source secrets from a secure secrets manager.

HYPIXEL_API_KEY=your_hypixel_api_key
PROXY_AUTH_TOKENS=comma,separated,tokens
PORT=3000
CACHE_DB_URL=postgres://user@localhost/database
